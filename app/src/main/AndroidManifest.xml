<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.mihassu.libraryhw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".App">

        <!--android:name="com.orm.SugarApp" или так: SugarContext.init(getApplicationContext());-->
        <meta-data android:name="DATABASE" android:value="sugar_orm.db" />
        <meta-data android:name="VERSION" android:value="1" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="ru.mihassu.libraryhw.lesson05.databases" />

        <activity android:name=".MainActivity"></activity>
        <activity android:name=".lesson03.Lesson03MainActivity"></activity>
        <activity android:name=".lesson04.Lesson04MainActivity">

        </activity>

        <activity android:name=".lesson04.MyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>